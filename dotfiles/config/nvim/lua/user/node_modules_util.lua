-- ====================
-- Node modules utility
-- ====================

-- Add node_modules folder to the PATH if it exists in the current directory.
local node_modules_dir = vim.env.PWD .. '/node_modules'
if (vim.fn.isdirectory(node_modules_dir))
then
    vim.env.PATH = vim.env.PATH .. ':' .. node_modules_dir
end
